<Window x:Class="AutoMouse.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入"
        Width="360" Height="180"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="PromptText" Text="请输入："
                   FontSize="13" Margin="0,0,0,10"/>

        <TextBox Grid.Row="1" x:Name="InputBox" FontSize="14" Padding="6"
                 KeyDown="InputBox_KeyDown"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="确定" Width="80" Padding="0,6" Margin="0,0,10,0" Click="OK_Click" IsDefault="True"/>
            <Button Content="取消" Width="80" Padding="0,6" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
